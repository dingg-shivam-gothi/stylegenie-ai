"use client";

export function PreferencesForm({
  onComplete,
  onBack,
}: {
  onComplete: (prefs: Record<string, unknown>) => void;
  onBack: () => void;
}) {
  return <div>Preferences form (Task 5)</div>;
}
